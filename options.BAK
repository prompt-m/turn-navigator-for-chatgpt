<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ChatGPT Turn Navigator 設定</title>
  <style>
    :root{
      --fg:#111; --muted:#666; --bd:#ddd; --bg:#fff; --pill:#f5f6f8;
      --label-col: 300px;      /* ラベル固定幅（200–300で調整） */
      --pins-h: 360px;         /* 付箋島の最大高さ */
      --radius: 12px;
    }
    @media (prefers-color-scheme: dark){
      :root{ --fg:#e7e7ea; --muted:#aaa; --bd:#3a3a3f; --bg:#1f1f22; --pill:#2a2a2d; }
    }

    /* base */
    body{
      margin:24px;
      font:16px/1.4 system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial;
      color:var(--fg);
      background:var(--bg);
      line-height: 1.0;
    }
    h1{ font-size:28px; margin:0 0 6px; }
    .desc{ color:var(--muted); margin:0 0 5px; }

    /* card */
    .card{
      position:relative;
      border:1px solid var(--bd);
      border-radius:var(--radius);
      background:var(--pill);
      padding:24px;
      padding-top:56px; /* 言語トグル分 */
    }

    /* lang switch */
    .lang-switch{
      position:absolute; top:12px; right:14px; z-index:2;
      display:flex; gap:8px;
    }
    .pill-btn{
      all:unset; cursor:pointer; display:inline-grid; place-items:center;
      padding:8px 14px; border:1px solid var(--bd); border-radius:5px	px; background:#f2f2f7;
      box-shadow:0 6px 24px rgba(0,0,0,.18);
    }
    .pill-btn:hover{ background:#fff; }
    @media (prefers-color-scheme: dark){
      .pill-btn{ background:#3a3a40; box-shadow:0 6px 24px rgba(0,0,0,.28); }
    }

    /* section block：左バー＋インデント */
    .section-block{
      display:grid; grid-template-columns: 12px 1fr; gap:10px; margin-bottom:20px;
    }
    .section-block::before{
      content:""; display:block; width:4px; margin-top:6px; border-radius:3px;
      background:color-mix(in srgb, var(--fg) 30%, transparent);
    }
    .section-inner{ display:flex; flex-direction:column; gap:5px; }/*gap:12px*/

    .section-title{ font-size:18px; font-weight:700; margin-top:10px; margin-bottom:4px; }

    /* セクション右肩のツールバー（使用量＋最新にする） */
    .section-head{
      display:flex; align-items:flex-end; gap:10px; flex-wrap:wrap;
      justify-content:space-between;
    }
    .section-tools{
      display:flex; align-items:center; gap:10px; margin-left:auto;
      white-space:nowrap;
    }
    .usage-label{
      padding:6px 10px; border:1px solid var(--bd); border-radius:8px; background:var(--bg);
      font:14px/1.2 system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial;
      opacity:.9;
    }

    .hint-row {
      display: flex;
      align-items: center; /* 垂直中央揃え */
      gap: 10px;           /* ２つの間の余白 */
    }

    .hint-row .hint {
      margin: 0;           /* デフォルトの<p>余白を消す */
      white-space: nowrap; /* 折り返さない */
    }

    .hint {
      opacity: 1;
      color: var(--muted);
      font-size: 14px;
      transition: opacity 0.25s;
      margin: 4px 0 8px;
    }
    .hint.show {
      opacity: 1;
    }
    .flash-msg {
      color: var(--fg);
      font-weight: 500;
      opacity: 0;
      transition: opacity 0.3s;
    }
    .flash-msg.show {
      opacity: 1;
    }

    /* 設定行：ラベル左固定／入力右フレキシブル */
    .settings{
      font:14px/1.6 system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial;
      display:grid; grid-template-columns: var(--label-col) 1fr;
      gap:14px; align-items:center;
      background:var(--bg); border:1px solid var(--bd); border-radius:10px;
      padding:4px 10px;/*14px 16px*/
    }
    .settings + .settings{ margin-top:10px; }
    label{ font-weight:600; }

    input[type="number"], input[type="text"]{
      width:220px; max-width:100%;
      padding:8px 10px; border-radius:10px; border:1px solid var(--bd);
      background:var(--bg); color:var(--fg);
    }
    input[type="checkbox"]{ cursor:pointer; }

    /* 保存帯（軽いスティッキー） */
    .section-actions{
      position:sticky; bottom:-4px;
      display:flex; gap:8px; align-items:center;
      padding-top:8px;
      background:linear-gradient(to top, var(--pill) 70%, transparent);
    }

    .btn{
      /* 旧: all:revert だと状況によって横幅が伸びることがある */
      /* ここから追加 */
      all:unset;
      display:inline-flex; align-items:center; justify-content:center;
      padding:8px 14px; border-radius:5px; border:1px solid var(--bd); background:var(--pill);
      font:inherit; cursor:pointer; box-shadow:0 2px 8px rgba(0,0,0,.12);
      user-select:none;
      /* ここまで */
    }

    /* ここから追加：ボタンスピナー */
    .btn.is-busy{
      position: relative;
      pointer-events: none;
      opacity: .85;
    }
    .btn.is-busy::after{
      content: "";
      position: absolute;
      right: 10px; top: 50%;
      width: 16px; height: 16px;
      margin-top: -8px;
      border-radius: 50%;
      border: 2px solid currentColor;
      border-right-color: transparent;
      animation: cgtn-spin 1s linear infinite;
    }
    @keyframes cgtn-spin { to { transform: rotate(360deg); } }
    /* ここまで */

    .btn:hover{ background: color-mix(in srgb, var(--pill) 90%, var(--fg) 10%); }
    .msg-inline{
      margin-left:6px; font-size:14px; color:var(--muted);
      opacity:0; transition:opacity .15s ease;
    }
    .msg-inline.show{ opacity:1; }

    /* 付箋アイランド */
    .island{
      background:var(--bg); border:1px solid var(--bd); border-radius:10px; padding:12px;
    }

    /* ここから追加：テーブルだけスクロール */
    .pins-wrap{ /* ラッパ：高さの上限は維持（カード内での視覚的まとまり） */
      max-height:var(--pins-h);
    }

    .cgtn-pins-table{
      width:100%;
      border-collapse:collapse;
      font-size:13px;
    }
    .cgtn-pins-table th, .cgtn-pins-table td{ padding:6px 8px; border-bottom:1px solid rgba(0,0,0,.08); }
    .cgtn-pins-table thead th{ position:sticky; top:0; background:var(--pill); z-index:1; }
    /* ここから追加：No. / チャット / 会話数 / 付箋数 / 更新 = 5% / 45% / 10% / 10% / 30% */
    .cgtn-pins-table th:nth-child(1), .cgtn-pins-table td:nth-child(1){ width:5%;  text-align:right; padding-right:10px; }
    .cgtn-pins-table th:nth-child(2), .cgtn-pins-table td:nth-child(2){ width:45%; word-break:break-word; white-space:normal; }
    .cgtn-pins-table th:nth-child(3), .cgtn-pins-table td:nth-child(3){ width:10%; text-align:right; }
    .cgtn-pins-table th:nth-child(4), .cgtn-pins-table td:nth-child(4){ width:10%; text-align:right; }
    .cgtn-pins-table th:nth-child(5), .cgtn-pins-table td:nth-child(5){ width:30%; text-align:center; vertical-align:middle; }
    /* 付箋数セルのボタン間隔 */
    .cgtn-pins-table td.count .btn.inline { margin-left: 10px; }
    /* 右寄せはそのまま */
    .cgtn-pins-table td.count { text-align: right; }
    .cgtn-pins-table .count .btn.del{ margin-left: 8px; }

    .cgtn-pins-scroll{
      overflow-y:auto;
      border:1px solid rgba(0,0,0,.08);
      border-radius:6px;
      max-height:280px;          /* 表だけスクロール */
      background:var(--bg);
    }

    #sync-usage {
      display: inline-block;
      min-width: 320px;        /* 好きな幅に。280–360px 目安 */
      text-align: right;
      white-space: nowrap;
    }

  }

    /* responsive */
    @media (max-width:720px){
      :root{ --label-col: 160px; --pins-h:min(50vh,360px); }
      .settings{ grid-template-columns: 1fr; }
    }

    /* 補助 */
    footer .desc{ margin-top:12px; }
    #cgtn-floater{ position:fixed; inset:0; pointer-events:none; z-index:9999; }
  </style>
</head>
<body>
  <h1 id="opt-title" data-i18n="opts.title"></h1>
  <p class="desc" id="opt-desc" data-i18n="opts.tips"></p>

  <form id="cgtn-options" class="card" autocomplete="off">
    <!-- 言語トグル（既存 i18n.js と互換ID） -->
    <div class="lang-switch">
      <button id="lang-ja" class="pill-btn" type="button" data-i18n="opts.lang.ja"></button>
      <button id="lang-en" class="pill-btn" type="button" data-i18n="opts.lang.en"></button>
    </div>


    <!-- ===== 付箋データ管理 ===== -->
    <div class="section-block">
      <div class="section-inner">

        <div class="section-head" style="display:flex;align-items:center;justify-content:space-between;">
          <div>
            <div class="section-title" data-i18n="options.pinsTitle">付箋データ管理</div>
            <div class="hint" data-i18n="options.pinsHint">各チャットの付箋を一覧。不要になったチャットは削除できます。</div>
          </div>
          <div class="section-tools" style="display:flex;align-items:center;gap:8px;">
            <span id="sync-usage" class="usage-label" style="min-width:18ch" title="chrome.storage.sync usage">sync...</span>
            <button id="pins-refresh" class="btn small" type="button">
              <span data-i18n="options.refreshTitles">最新にします</span>
            </button>
          </div>
        </div>

        <div class="island" style="margin-top:8px;">
          <div class="pins-wrap">
            <div class="table-scroll">
              <table id="pins-table" class="cgtn-pins-table">
                <thead>
                  <tr>
                    <th style="width:72px;">No.</th>
                    <th data-i18n="options.thChat">チャット</th>
                    <th style="width:120px;" data-i18n="options.thTurns">会話数</th>
                    <th style="width:120px;" data-i18n="options.thCount">付箋数</th>
                    <th style="width:180px;" data-i18n="options.thUpdated">更新</th>
                  </tr>
                </thead>
                <tbody id="pins-tbody"></tbody>
              </table>
            </div>
          </div>

<!--
          <div class="pins-wrap" style="border:1px solid var(--hair);border-radius:8px;max-height:320px;overflow:auto;">
            <table class="pins-table" style="width:100%;border-collapse:separate;border-spacing:0;">
              <thead style="position:sticky;top:0;background:var(--panel-bg);z-index:1;">
                <tr>
                  <th style="width:56px;text-align:right;padding:8px 10px;">No.</th>
                  <th style="text-align:left;padding:8px 10px;">
                    <span data-i18n="options.thChat">チャット</span>
                  </th>
                  <th style="width:80px;text-align:right;padding:8px 10px;">
                    <span data-i18n="options.thCount">付箋数</span>
                  </th>
                  <th style="width:54px;text-align:center;">
                    <span data-i18n="options.thOps">操作</span>
                  </th>
                  <th style="width:210px;text-align:center;padding:8px 10px;">
                    <span data-i18n="options.thUpdated">更新</span>
                  </th>
                </tr>
              </thead>
              <tbody id="pins-tbody"></tbody>
            </table>
          </div>
-->

        </div>

      </div>
    </div>
    <!-- ===== /付箋データ管理 ===== -->



    <!-- ===== 一覧パネルの表示設定（保存帯つき） ===== -->
    <div class="section-block">
      <div class="section-inner">
        <div class="section-title" data-i18n="options.listTitle"></div>

        <div class="settings">
          <label for="listMaxChars"><b id="lbl-listMaxChars" data-i18n="options.listMaxChars"></b></label>
          <input id="listMaxChars" name="listMaxChars" type="number" min="10" max="200" step="1" />
        </div>

        <div class="settings">
          <label for="listFontSize"><b id="lbl-listFontSize" data-i18n="options.listFontSize"></b></label>
          <input id="listFontSize" name="listFontSize" type="number" min="8" max="24" step="1" />
        </div>

        <div class="section-actions">
          <button id="saveList"  class="btn" type="button" data-i18n="options.saveBtn"></button>
          <button id="resetList" class="btn" type="button" data-i18n="options.resetBtn"></button>
          <span  id="msg-list"  class="msg-inline" aria-live="polite"></span>
        </div>
      </div>
    </div>

    <!-- ===== 詳細設定（保存帯つき） ===== -->
    <div class="section-block">
      <div class="section-inner">
        <div class="section-title" data-i18n="options.detailTitle"></div>

        <div class="settings">
          <label for="showViz"><b id="lbl-showViz" data-i18n="nav.viz"></b></label>
          <div>
            <label style="display:inline-flex;gap:8px;align-items:center;cursor:pointer;">
              <input id="showViz" name="showViz" type="checkbox" />
              <span class="hint" id="hint-showViz" data-i18n="nav.viz"></span>
            </label>
          </div>
        </div>

        <div class="settings">
          <label for="centerBias"><b id="lbl-centerBias" data-i18n="options.centerBias"></b></label>
          <div>
            <input id="centerBias" name="centerBias" type="number" step="0.01" min="0" max="1" />
            <div class="hint" id="hint-centerBias" data-i18n="options.centerBiasHint"></div>
          </div>
        </div>

        <div class="settings">
          <label for="eps"><b id="lbl-eps" data-i18n="options.eps"></b></label>
          <div>
            <input id="eps" name="eps" type="number" step="1" min="0" max="120" />
            <div class="hint" id="hint-eps" data-i18n="options.epsHint"></div>
          </div>
        </div>

        <div class="settings">
          <label for="lockMs"><b id="lbl-lockMs" data-i18n="options.lockMs"></b></label>
          <input id="lockMs" name="lockMs" type="number" step="10" min="0" max="3000" />
        </div>

        <div class="section-actions">
          <button id="saveAdv"  class="btn" type="button" data-i18n="options.saveBtn"></button>
          <button id="resetAdv" class="btn" type="button" data-i18n="options.resetBtn"></button>
          <span  id="msg-adv"  class="msg-inline" aria-live="polite"></span>
        </div>
      </div>
    </div>
  </form>

  <footer><p class="desc hint" id="buildInfo" style="margin-top:12px;"></p></footer>
  <div id="cgtn-floater" aria-live="polite" aria-atomic="true"></div>

  <!-- 既存ロジックを読み込み -->
  <script src="i18n.js"></script>
  <script src="shared.js"></script>
  <script src="options.js"></script>


</body>
</html>
